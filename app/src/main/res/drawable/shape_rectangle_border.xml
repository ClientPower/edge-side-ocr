<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 填充色（内容区域颜色）-->
<!--    <solid android:color="" />-->

    <!-- 边框 -->
    <stroke
        android:dashWidth="6dp"
        android:dashGap="2dp"
        android:width="1dp"
        android:color="@color/white" /> <!-- 你要的边框色 -->

    <!-- 圆角 -->
    <corners android:radius="12dp" />
</shape>